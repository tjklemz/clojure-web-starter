#!/bin/bash

export GEM_HOME=$PWD/gems

mkdir -p $GEM_HOME

LC_ALL=en_US.UTF-8 gem install bundler

$GEM_HOME/bin/bundle install
$GEM_HOME/bin/bundle exec compass compile

lein with-profile production compile :all
